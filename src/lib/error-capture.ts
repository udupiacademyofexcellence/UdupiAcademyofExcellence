// Stub — SSR error capture removed for static SPA.
export function consumeLastCapturedError(): unknown {
  return undefined;
}
export function describeError(_error: unknown): string {
  return "";
}
